{
  "name": "blue-carbon-registry-contracts",
  "version": "1.0.0",
  "description": "Solidity contracts for the Blockchain-Based Blue Carbon Registry and MRV System",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
